node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Vite
.vite/

# Hero assets fetched by scripts/fetch-assets.mjs at build time.
# manifest.json is committed as a placeholder so dev works without a fetch.
public/assets/met/
public/assets/audio/
public/assets/hero.mp4

# Built/cloned third-party desktop app — see tools/README.md for re-install steps
tools/1code/

# Local env files (Mapbox token, etc.) — keep .env.example committed
.env
.env.local
.env.*.local

# Project-scoped MCP config (contains API keys). The template
# .mcp.json.example is committed.
.mcp.json
